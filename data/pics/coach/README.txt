Place coach portrait images here.

Filenames must match the "image" column in /data/coach-relentless.csv.
Expected filenames:
  - florent-blanc.jpg
  - adrien-plastre.jpg

If an image is missing (or the "image" column is empty for a row),
the loader falls back to the coach's initials (from the "initials" column).

Recommended format: square crop, 256x256 or larger, JPG or PNG.
